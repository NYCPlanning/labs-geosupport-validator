Drag and drag any number of txt files:

<DropZone
  @url='#'
  @autoProcessQueue={{true}}
  @uploadMultiple={{true}}
  @addedfile={{action this.handleFile}}
>
  drop items here...
</DropZone>

{{#each mismatches as |mismatch|}}
  Problem with: {{mismatch.line}} <br/>
{{/each}}
